pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
}

plugins {
    id "com.android.application" version "4.1.0" apply false
    id "org.jetbrains.kotlin.android" version "1.9.0" apply false
}

rootProject.name = 'readsms'
